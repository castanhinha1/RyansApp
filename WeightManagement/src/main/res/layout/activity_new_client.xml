<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/trainer_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.parse.starter.ViewControllers.TrainerView"
    android:background="#3a8f3e">

    <TextView
        android:text="Clients"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:id="@+id/clientsTextView"
        android:textSize="20sp"
        android:textColor="#FFF"
        android:fontFamily="sans-serif-condensed" />

    <ListView
        android:layout_width="match_parent"
        android:layout_below="@+id/clientsTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp"
        android:id="@+id/newClientsListView"
        android:layout_height="match_parent" />

</RelativeLayout>